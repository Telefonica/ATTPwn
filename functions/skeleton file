#Title: Name-function
#Technique: ID Tech
#Tactic: ID Tactic

function invoke-namefunction
    {
    <#
    .SYNOPSIS
    
    .DESCRIPTION
    Attack Commands: Run with command_prompt! Elevation Required (e.g. root or admin)

    .LINK

    
    #>
    $good = $false
    $results = $null

    if($PSVersionTable.PSVersion.Major -match "[1-5]")
    {
      # Windows with powershell between v1 and v5.1
    } else {
      if ($IsWindows)
      {
        # Windows with Powershell v5.1+
      } elseif ($isMacOs) {
        # MacOs with Powershell
      } elseif ($isLinux) {
        #Linux distribution with Powershell
      }
    }

    return @{results=$results;success=$good}
    }


#Main program
$execute = {
  
}

#GiveMeData if needed

#get-credentials
$execute = invoke-namefunction

#PutData if needed
  # if($execute.success)
  # {
    
  # }

return @{results=$execute.results;success=$execute.success}